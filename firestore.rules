rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /finance_data/{document=**} {
      allow read, write: if true; // Open for now to allow initial setup/testing
    }
  }
}
